# 购物车与订单模块代码分析

> 依据文件：[AuthFilter.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/filter/AuthFilter.java)、[CartServlet.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/CartServlet.java)、[OrderServlet.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/OrderServlet.java)、[CartDao.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/CartDao.java)、[OrderDao.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java)

---

## 1. 未登录用户访问 `/cart/add`（AJAX）与 `/order/checkout`（浏览器）的差异

两者都会先经过 `AuthFilter#doFilter`，但在过滤器内分支不同，结果完全不同。

### 路径 A：AJAX `POST /cart/add`
- 文件/方法：[AuthFilter#doFilter](file:///d:/Agsb/gsb3517/src/main/java/com/shop/filter/AuthFilter.java#L26-L67)
- `path = /cart/add` 不在 `PUBLIC_PATHS` 中，`isPublicPath` 返回 false。
- `session` 为 null 或 `user == null`，进入需要登录分支（第 53 行）。
- 因为 AJAX 请求带 `X-Requested-With: XMLHttpRequest`，命中 [AuthFilter.java#L55-L61](file:///d:/Agsb/gsb3517/src/main/java/com/shop/filter/AuthFilter.java#L55-L61)：
  - 返回 HTTP 401
  - Body：`{"success":false,"message":"请先登录"}`
- **不会**进入 `CartServlet#doPost`，前端可在 AJAX 回调里提示登录。

### 路径 B：浏览器 `GET /order/checkout`
- 同样命中 [AuthFilter.java#L53](file:///d:/Agsb/gsb3517/src/main/java/com/shop/filter/AuthFilter.java#L53)，但请求头无 `X-Requested-With`。
- 进入 [AuthFilter.java#L62](file:///d:/Agsb/gsb3517/src/main/java/com/shop/filter/AuthFilter.java#L62)：`response.sendRedirect(contextPath + "/user/login")`，HTTP 302 跳转到登录页。
- 同样**不会**进入 [OrderServlet#doGet](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/OrderServlet.java#L29-L86) 的 `/checkout` 分支。

> 总结：过滤器统一拦截，但根据是否 XHR 给出 **JSON 401** 或 **302 重定向** 两种不同响应；这两个 servlet 内部对 user==null 的二次判断在过滤器存在时已无机会触发。

---

## 2. 用户下单（POST `/order/create`）完整流程

入口：[OrderServlet#doPost case "/create"](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/OrderServlet.java#L100-L154)

### 2.1 校验
- 登录校验：[OrderServlet.java#L92-L96](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/OrderServlet.java#L92-L96)（实际上被 `AuthFilter` 已经挡住）。
- 收货人 `receiver` 非空：[L106-L109](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/OrderServlet.java#L106-L109)
- 联系电话 `phone` 非空：[L110-L113](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/OrderServlet.java#L110-L113)
- 收货地址 `address` 非空：[L114-L117](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/OrderServlet.java#L114-L117)
- 购物车非空：[L119-L123](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/OrderServlet.java#L119-L123)，调用 [CartDao#findByUserId](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/CartDao.java#L12-L26)。
- **没有**做：手机号格式、库存是否足够、商品是否下架（`findByUserId` 里有 `p.status=1` 的过滤但仅过滤展示，未阻止下单）。

### 2.2 订单明细的商品价格 / 名称来源
循环 [OrderServlet.java#L125-L136](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/OrderServlet.java#L125-L136)：
- `productName / productPrice / productImage` 都来自 `CartItem`（即 `cartDao.findByUserId` 查询时 `JOIN products p` 取到的 `p.name / p.price / p.image`，见 [CartDao.java#L14-L15](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/CartDao.java#L14-L15)）。
- `totalAmount` = 各 `CartItem.subtotal` 累加（`subtotal` 由 `productPrice * quantity` 计算，模型层完成）。
- 即 **价格/名称是下单当下从 products 表实时读出的**，并写入 `order_items`，相当于价格快照。

### 2.3 `createOrder` 事务做了哪几件事
方法：[OrderDao#createOrder](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java#L13-L74)
1. 取连接、`setAutoCommit(false)` 开启事务（[L16-L17](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java#L16-L17)）。
2. **INSERT orders**，使用 `RETURN_GENERATED_KEYS` 拿到 `orderId`（[L19-L34](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java#L19-L34)）。
3. **批量 INSERT order_items**：循环 `addBatch` 后 `executeBatch`（[L36-L48](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java#L36-L48)）。
4. **批量 UPDATE products**：`stock = stock - ?, sales = sales + ? WHERE id = ? AND stock >= ?`（[L50-L60](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java#L50-L60)）。
5. `conn.commit()` 提交（[L62](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java#L62)）。异常时 `rollback`（[L65-L67](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java#L65-L67)）。
6. finally 中重置 autoCommit 并关闭连接（[L69-L73](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java#L69-L73)）。

### 2.4 购物车的变化
`createOrder` 成功返回后，[OrderServlet.java#L149](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/OrderServlet.java#L149) 调用 [CartDao#clearByUserId](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/CartDao.java#L83-L92) 清空当前用户购物车。

> 注意：`clearByUserId` 在另一连接执行，**不在订单事务内**。若清空失败但订单已提交，会出现订单已生成、购物车残留的不一致。

---

## 3. 库存只有 2，购买 5 时 `UPDATE ... WHERE stock >= ?` 的行为

相关代码：[OrderDao.java#L50-L60](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java#L50-L60)

```sql
UPDATE products SET stock = stock - ?, sales = sales + ? WHERE id = ? AND stock >= ?
```
- 参数：`quantity=5, quantity=5, productId, quantity=5`。
- 当前 `stock=2`，条件 `stock >= 5` 不成立，**该行不会被更新**，affected rows = 0；不会把库存改成负数。

### 代码是否检查影响行数？
- 没有。代码只调用了 `stockPs.executeBatch()`（[L60](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java#L60)），未读取返回的 `int[]`，也没有判断 0 行 → 抛异常 → 回滚。

### 后果
- `INSERT orders` 与 `INSERT order_items` 已在事务里执行，commit 成功 → **超卖**：订单生成、库存未扣、`sales` 也未增加；用户付款后无货可发。
- 同时 `clearByUserId` 把购物车清掉，用户也看不到失败痕迹。

### 修复建议
1. 接收 `executeBatch` 的返回值并校验：若任何元素 `<= 0`（或 `Statement.EXECUTE_FAILED`/`SUCCESS_NO_INFO` 之外的 0），抛异常以触发回滚。
   ```java
   int[] rows = stockPs.executeBatch();
   for (int r : rows) {
       if (r <= 0) throw new SQLException("库存不足");
   }
   ```
2. 下单前先按 `productId` 锁行（`SELECT ... FOR UPDATE`）或在业务层校验库存。
3. 将 `cartDao.clearByUserId` 也纳入同一事务（让 `OrderDao` 接收 `Connection` 或在外层开事务），保证一致性。

---

## 4. `/cart/add` 与 `/cart/update` 对 `quantity` 的校验差异

- `/cart/add`（[CartServlet.java#L53-L70](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/CartServlet.java#L53-L70)）：
  - `int quantity = getInt(req, "quantity", 1)`；
  - **只校验 productId 非空**，未对 `quantity` 做范围检查；负数、0、超大值都会直传 DAO。
- `/cart/update`（[CartServlet.java#L71-L83](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/CartServlet.java#L71-L83)）：
  - 校验 `id != null && quantity >= 1`，**拒绝 0 和负数**。

### 配合 `INSERT ... ON DUPLICATE KEY UPDATE quantity = quantity + ?`（[CartDao#insert](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/CartDao.java#L44-L56)）

传入 `quantity = -3` 时：
- 若该 `(user_id, product_id)` 不存在 → INSERT 一条 `quantity = -3` 的记录（数据库列若为无符号 INT 会报错；若是普通 INT 则真的存进去）。
- 若已存在 → 触发 `ON DUPLICATE KEY UPDATE`，`quantity = quantity + (-3)`，相当于**用 add 接口悄悄减数量**。
- 极端情况下还能让 `quantity` 变成 0 或负数，绕开 `/cart/update` 的 `quantity < 1` 校验，进入结算时算出错误的小计/总价。

### 修复建议
- `/cart/add` 同样要求 `quantity >= 1` 并设置上限；
- DAO 层或 SQL 加防御：`quantity > 0` 才执行；或对 `quantity = quantity + ?` 加 `GREATEST(quantity + ?, 1)`；
- 数据库列建议 `UNSIGNED` 或加 `CHECK (quantity > 0)`。

---

## 5. `createOrder` 中三个 `PreparedStatement` 的关闭情况

定位：[OrderDao#createOrder](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java#L13-L74)
- `ps`（INSERT orders）— [L21](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java#L21)
- `itemPs`（INSERT order_items）— [L38](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java#L38)
- `stockPs`（UPDATE products）— [L52](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java#L52)

以及 `keys = ps.getGeneratedKeys()`（ResultSet）也未关闭。

### 是否被关闭？
**都没有**显式 `close()`，也没有放进 try-with-resources。`finally` 块只做了 `conn.setAutoCommit(true); conn.close()`（[L70-L72](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java#L70-L72)），并未关闭 3 个 `PreparedStatement` 与 `ResultSet`。

### 与项目其他方法的对比
其他方法如 [CartDao#insert](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/CartDao.java#L44-L56)、[OrderDao#findById](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java#L113-L130) 都使用了：
```java
try (Connection conn = ...; PreparedStatement ps = ...) { ... }
```
JDBC 规范规定关闭 `Connection` 一般会同时关闭其上的 `Statement/ResultSet`，但这取决于驱动实现：
- 在使用连接池（HikariCP、Druid 等，见 `DBUtil`）时，`conn.close()` 实际是把连接归还连接池，**底层物理连接并没有真正关闭**。许多池实现并不会自动关闭你在该连接上创建的 `Statement`，于是会出现：
  - **游标/Statement 句柄泄漏**（MySQL 端 `prepared_stmt_count` 持续上升，触发 `max_prepared_stmt_count` 报错）。
  - **ResultSet 滞留**，占用内存/数据库资源。
  - 连接归还后再被复用时携带旧的未关闭语句，可能产生奇怪的状态。
- 在异常路径（例如批量更新抛 SQLException）下，由于既没有 try-with-resources 也没有 finally close，泄漏更明显。

### 修复建议
将三个语句也写成 try-with-resources（注意嵌套顺序，因为依赖 `orderId`）：

```java
try (Connection conn = DBUtil.getConnection()) {
    conn.setAutoCommit(false);
    long orderId;
    try (PreparedStatement ps = conn.prepareStatement(orderSql, Statement.RETURN_GENERATED_KEYS)) {
        // setXxx ...
        ps.executeUpdate();
        try (ResultSet keys = ps.getGeneratedKeys()) {
            if (!keys.next()) throw new SQLException("创建订单失败");
            orderId = keys.getLong(1);
        }
    }
    try (PreparedStatement itemPs = conn.prepareStatement(itemSql)) { ... itemPs.executeBatch(); }
    try (PreparedStatement stockPs = conn.prepareStatement(stockSql)) {
        // setXxx ...
        int[] rows = stockPs.executeBatch();
        for (int r : rows) if (r <= 0) throw new SQLException("库存不足");
    }
    conn.commit();
} catch (SQLException e) {
    // rollback
}
```

这样既能保证语句关闭，也能顺手解决问题 3 的库存校验。

---

## 总结一张表

| 问题 | 关键依据 | 结论 |
|------|----------|------|
| 1 未登录访问 | [AuthFilter#doFilter](file:///d:/Agsb/gsb3517/src/main/java/com/shop/filter/AuthFilter.java#L26-L67) | AJAX → 401 JSON；浏览器 → 302 跳登录 |
| 2 下单流程 | [OrderServlet#doPost](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/OrderServlet.java#L100-L154) + [OrderDao#createOrder](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java#L13-L74) | 校验三字段+购物车非空 → 价格名称取自购物车 join 出的 products → 事务内 INSERT orders / order_items / UPDATE stock → 事务外 clearByUserId |
| 3 超卖 | [OrderDao.java#L50-L60](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java#L50-L60) | 0 行更新但未检查，订单照样提交，超卖 |
| 4 quantity 校验差异 | [CartServlet#doPost](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/CartServlet.java#L53-L83) + [CartDao#insert](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/CartDao.java#L44-L56) | add 不校验 → 负数会通过 ON DUPLICATE KEY UPDATE 减少甚至变负 |
| 5 资源泄漏 | [OrderDao#createOrder](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java#L13-L74) | 三个 PreparedStatement 与 ResultSet 均未关闭，连接池场景下会语句句柄泄漏 |
