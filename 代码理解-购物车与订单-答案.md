# 购物车与订单模块代码分析答案

## 问题 1：未登录用户 AJAX 请求 `/cart/add` vs 浏览器访问 `/order/checkout`

### 两者结果的不同

| 场景 | 响应方式 | 状态码 | 返回内容 |
|------|----------|--------|----------|
| AJAX 请求 `/cart/add` | 返回 JSON | 401 UNAUTHORIZED | `{"success":false,"message":"请先登录"}` |
| 浏览器访问 `/order/checkout` | 重定向 | 302 | 跳转到 `/user/login` 页面 |

### 代码路径

**AJAX 请求 `/cart/add` 路径：**

1. [AuthFilter.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/filter/AuthFilter.java) → `doFilter()` 方法（第 27-67 行）
   - 第 36-39 行：`isPublicPath(path)` 判断，`/cart/add` 不在 `PUBLIC_PATHS` 中，不放行
   - 第 41-42 行：获取 session 和 user，未登录则 `user == null`
   - 第 53 行：`user == null && !isPublicPath(path)` 条件成立
   - 第 55-60 行：检测到 `X-Requested-With: XMLHttpRequest` 请求头，返回 JSON 格式的 401 响应
   - **请求不会到达 CartServlet**，直接在 Filter 层被拦截

**浏览器访问 `/order/checkout` 路径：**

1. [AuthFilter.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/filter/AuthFilter.java) → `doFilter()` 方法（第 27-67 行）
   - 第 36-39 行：`isPublicPath(path)` 判断，`/order/checkout` 不在 `PUBLIC_PATHS` 中，不放行
   - 第 41-42 行：获取 session 和 user，未登录则 `user == null`
   - 第 53 行：`user == null && !isPublicPath(path)` 条件成立
   - 第 55 行：浏览器普通请求没有 `X-Requested-With` 头，不满足 AJAX 条件
   - 第 62 行：执行 `response.sendRedirect()` 重定向到登录页
   - **请求不会到达 OrderServlet**，直接在 Filter 层被拦截

**依据：** [AuthFilter.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/filter/AuthFilter.java) 第 52-63 行

---

## 问题 2：下单（POST `/order/create`）完整流程

### 一、做了哪些校验

1. **登录校验**（双重校验）
   - [AuthFilter.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/filter/AuthFilter.java) → `doFilter()`：过滤器层校验
   - [OrderServlet.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/OrderServlet.java) → `doPost()` 第 92-96 行：Servlet 层再次校验 `getUser()`

2. **收货信息校验**
   - [OrderServlet.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/OrderServlet.java) → `doPost()` 第 106-117 行
   - 收货人 `receiver` 非空校验
   - 联系电话 `phone` 非空校验
   - 收货地址 `address` 非空校验

3. **购物车非空校验**
   - [OrderServlet.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/OrderServlet.java) → `doPost()` 第 119-123 行
   - 查询购物车，若为空则返回错误

### 二、订单明细的商品价格/名称从哪取

**来源：数据库 `products` 表，通过购物车查询时关联获取**

- [CartDao.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/CartDao.java) → `findByUserId()` 第 12-26 行
- SQL：`SELECT c.*, p.name as product_name, p.price as product_price, p.image as product_image ... FROM cart_items c JOIN products p ON c.product_id = p.id WHERE ...`
- 下单时通过 `cartDao.findByUserId(user.getId())` 查询购物车，同时关联查出商品名称、价格、图片
- 即：每次查询购物车都会实时从 `products` 表获取最新的商品信息，而非存在购物车表中

**依据：** [CartDao.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/CartDao.java) 第 14-15 行

### 三、`createOrder` 事务里依次做了哪几件事

[OrderDao.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java) → `createOrder()` 第 13-74 行

**事务内 4 件事，按顺序执行：**

1. **插入订单主表**（`orders` 表）
   - 第 20-30 行：执行 `INSERT INTO orders ...`
   - 使用 `Statement.RETURN_GENERATED_KEYS` 获取自增主键

2. **获取订单 ID**
   - 第 32-34 行：通过 `getGeneratedKeys()` 获取新插入订单的 ID

3. **批量插入订单明细**（`order_items` 表）
   - 第 37-48 行：循环遍历订单项，使用 `addBatch()` 批量插入

4. **批量更新商品库存和销量**（`products` 表）
   - 第 51-60 行：`UPDATE products SET stock = stock - ?, sales = sales + ? WHERE id = ? AND stock >= ?`
   - 使用 `addBatch()` 批量扣减库存、增加销量

5. **提交事务**
   - 第 62 行：`conn.commit()`

**依据：** [OrderDao.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java) 第 19-62 行

### 四、购物车如何变化

**下单成功后清空购物车**

- [OrderServlet.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/OrderServlet.java) → `doPost()` 第 147-153 行
- 第 148 行：调用 `orderDao.createOrder()` 创建订单（事务内）
- 第 149 行：调用 `cartDao.clearByUserId(user.getId())` 清空购物车（事务外！）
- 清空操作使用 `DELETE FROM cart_items WHERE user_id = ?`

**注意：清空购物车不在 `createOrder` 的事务中**，如果订单创建成功但清空购物车失败，会出现"订单已创建但购物车还在"的不一致状态。

**依据：** [OrderServlet.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/OrderServlet.java) 第 147-150 行

---

## 问题 3：库存只有 2 件却下单买 5 件

### `UPDATE ... WHERE stock >= ?` 会怎样

**该条 UPDATE 影响行数为 0**

- SQL：`UPDATE products SET stock = stock - ?, sales = sales + ? WHERE id = ? AND stock >= ?`
- 当库存只有 2 件，购买 5 件时，`stock >= 5` 条件不满足
- 数据库不更新任何行，返回受影响行数 = 0

### 代码检查影响行数了吗？

**没有检查！**

- [OrderDao.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java) → `createOrder()` 第 60 行
- 使用 `stockPs.executeBatch()` 执行批量更新，返回值是 `int[]` 数组
- 代码**完全没有接收和检查返回值**，直接继续执行
- 即使某些商品库存不足导致更新失败（影响 0 行），代码也感知不到

**依据：** [OrderDao.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java) 第 60 行

### 会导致什么后果

**超卖问题（Oversell）：**

1. 订单创建成功，状态为 `PENDING`
2. 用户看到"下单成功"的提示
3. 该商品的库存**没有被扣减**（因为 stock >= ? 条件不满足）
4. 销量也没有增加
5. 但订单和订单明细都已经插入数据库
6. 商家看到订单后发现库存不足，无法发货

**事务虽然提交了，但数据不一致：订单存在但库存未扣减。**

### 怎么修

**方案 1：检查批量更新的返回值（推荐，最小改动）**

```java
int[] results = stockPs.executeBatch();
for (int i = 0; i < results.length; i++) {
    if (results[i] != 1) {
        throw new SQLException("库存不足，下单失败");
    }
}
```
只要有一条更新影响行数不是 1，就抛出异常触发回滚。

**方案 2：下单前先查询并校验库存**

在 Servlet 层先查询每个商品的库存，校验是否足够。但注意：这只能做前置校验，不能替代数据库层面的 `WHERE stock >= ?` 乐观锁，因为并发场景下可能出现"检查时库存够，扣减时不够"的情况。

**方案 3：使用 SELECT ... FOR UPDATE 悲观锁**

在事务内先 `SELECT stock FROM products WHERE id = ? FOR UPDATE` 锁定行，再判断和更新。并发安全性更高，但性能较差。

**最佳实践：方案 1（数据库层乐观锁 + 检查返回值） + 前置库存校验（用户体验）**

---

## 问题 4：`/cart/add` 和 `/cart/update` 对 `quantity` 的校验差异

### 校验差异对比

| 接口 | quantity 默认值 | 范围校验 | 能否传负数 |
|------|----------------|----------|------------|
| `/cart/add` | 解析失败时用 `1` | **无校验** | **可以传负数** |
| `/cart/update` | 解析失败时用 `1` | `quantity < 1` 则报错 | 不能传负数 |

**`/cart/add` 校验逻辑：**
- [CartServlet.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/CartServlet.java) → `doPost()` 第 53-70 行
- 第 55 行：`int quantity = getInt(req, "quantity", 1)`
- **没有**对 quantity 的正负或大小做任何校验

**`/cart/update` 校验逻辑：**
- [CartServlet.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/CartServlet.java) → `doPost()` 第 71-83 行
- 第 73 行：`int quantity = getInt(req, "quantity", 1)`
- 第 74 行：`if (id == null || quantity < 1)` 有明确的 `quantity < 1` 校验

**依据：** [CartServlet.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/controller/CartServlet.java) 第 53-83 行

### 结合 `ON DUPLICATE KEY UPDATE quantity = quantity + ?`，传入负数会发生什么

**SQL：** `INSERT INTO cart_items (user_id, product_id, quantity) VALUES (?, ?, ?) ON DUPLICATE KEY UPDATE quantity = quantity + ?`

**传入负数的后果：**

1. **如果商品不在购物车中：**
   - 执行 INSERT，直接把负数 quantity 写入数据库
   - 购物车数量为负数，逻辑不合理

2. **如果商品已在购物车中：**
   - 执行 UPDATE，`quantity = quantity + 负数` → 数量减少
   - 如果负数的绝对值 > 当前数量 → **quantity 会变成负数**
   - 购物车数量为负，订单计算总价时也会出现负数

**风险：**
- 购物车数量为负数
- 商品总价（subtotal）可能为负数
- 下单时订单总金额可能为负数
- 属于业务逻辑漏洞

**修复建议：** 在 `/cart/add` 处增加 `quantity >= 1` 的校验，与 `/cart/update` 保持一致。

**依据：** [CartDao.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/CartDao.java) 第 44-56 行

---

## 问题 5：`createOrder` 里的三个 `PreparedStatement` 有没有被关闭

### 有没有被关闭？

**没有显式关闭！**

[OrderDao.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java) → `createOrder()` 方法中有三个 `PreparedStatement`：

1. `ps` — 插入订单（第 21 行创建）
2. `itemPs` — 插入订单明细（第 38 行创建）
3. `stockPs` — 更新库存（第 52 行创建）
4. 还有一个 `ResultSet keys`（第 32 行）

**全部没有被显式关闭。** 只有 `Connection` 在 `finally` 块中被关闭（第 70-72 行）。

**依据：** [OrderDao.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/OrderDao.java) 第 13-74 行

### 和项目里其他用 try-with-resources 的方法相比有什么隐患

**其他方法的写法（正确示例）：**

[CartDao.java](file:///d:/Agsb/gsb3517/src/main/java/com/shop/dao/CartDao.java) 中所有方法都使用 **try-with-resources**，例如 `findByUserId()` 第 16-21 行：
```java
try (Connection conn = DBUtil.getConnection();
     PreparedStatement ps = conn.prepareStatement(sql)) {
    ps.setLong(1, userId);
    try (ResultSet rs = ps.executeQuery()) {
        while (rs.next()) list.add(mapRow(rs));
    }
}
```
Connection、PreparedStatement、ResultSet 都会自动关闭。

**隐患对比：**

| 问题 | try-with-resources（其他方法） | createOrder 手动管理 |
|------|-------------------------------|---------------------|
| Connection 关闭 | 自动关闭 | finally 手动关闭 ✓ |
| PreparedStatement 关闭 | 自动关闭 | **未关闭 ✗** |
| ResultSet 关闭 | 自动关闭 | **未关闭 ✗** |
| 异常场景下资源释放 | 保证释放 | 可能漏释放 |
| 代码简洁性 | 简洁 | 冗长易错 |

**具体隐患：**

1. **资源泄漏（Resource Leak）**
   - `PreparedStatement` 和 `ResultSet` 都属于 JDBC 资源，需要显式关闭
   - 虽然部分 JDBC 驱动在 `Connection.close()` 时会关闭关联的 Statement，但这不是 JDBC 规范强制要求的
   - 在数据库连接池（如 Druid、HikariCP）场景下，`Connection.close()` 通常只是**归还连接**而非真正关闭，Statement 可能不会被释放

2. **游标泄漏（Cursor Leak）**
   - 每个 `PreparedStatement` 和 `ResultSet` 在数据库端可能对应一个游标
   - 长期不关闭会导致数据库端游标数耗尽，报错 `ORA-01000: maximum open cursors exceeded`（Oracle）或类似错误

3. **内存泄漏**
   - 驱动层面的 Statement 对象无法被 GC 回收
   - 长时间运行后内存占用持续升高

4. **异常路径下更严重**
   - 如果 `executeBatch()` 抛出异常，代码直接跳到 catch 块 rollback，再到 finally 关连接
   - 中间创建的 `ps`、`itemPs`、`stockPs`、`keys` 都没有机会关闭
   - 虽然连接最终会关闭，但在连接池场景下这些 Statement 可能"附着"在连接上被反复复用，累积泄漏

**修复建议：** 使用嵌套的 try-with-resources 重写 `createOrder` 方法，确保所有 JDBC 资源都被正确关闭。
